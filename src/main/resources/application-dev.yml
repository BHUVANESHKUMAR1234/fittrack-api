spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/fittrack_dev

logging:
  level:
    com.fittrack: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
